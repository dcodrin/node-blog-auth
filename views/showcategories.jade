extends layout

block content
    h1 #{title}
    ul.list-group
        if categories
            each category, i in categories
                li.list-group-item
                    a(href='/categories/show/#{category}') #{category}